# PromptOS v4.2 — Requirement Graph

Typed, traceable, fail-closed semantic substrate. Twenty node types, sixteen
edge types, ten enforced invariants. A blocked node can never produce
VERIFIED COMPLETE. Contamination propagates in both directions along
dependency edges.
