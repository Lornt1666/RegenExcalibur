# Pull Request Gate

The RC pull request must remain draft until:

- branch CI completes;
- failures are diagnosed at the exact head;
- critical review defects are resolved;
- licence status is reviewed;
- release claims match evidence;
- the owner explicitly approves readiness for merge.

No auto-merge is requested by PromptOS RC1.
